<TaskLayout 
  title="Введение в React"
  description="Создайте свой первый React элемент используя createElement API. Это поможет понять основы работы React до изучения JSX."
>

<TaskObjective>

Создайте React элемент с заголовком "Привет из React!" и отобразите его на странице, используя только JavaScript API React.

</TaskObjective>

<Info>

**React элемент** — это описание того, что должно появиться на экране. React работает декларативно: мы описываем "что хотим", а не "как сделать".

Основные API которые нужно знать:
- `React.createElement()` — создает React элемент в памяти
- `ReactDOM.createRoot()` — создает корневой контейнер для React
- `root.render()` — отображает элемент в DOM

</Info>

<CodeExample title="Пример использования React.createElement">

```javascript
// Создание простого React элемента
const element = React.createElement(
  'h1',                    // тип HTML элемента
  null,                    // пропсы (свойства)
  'Привет из React!'       // содержимое элемента
);

// Создание корневого контейнера
const root = ReactDOM.createRoot(
  document.getElementById('root')
);

// Рендеринг элемента в DOM
root.render(element);
```

</CodeExample>

<TaskLinks 
  title="📚 Материалы для изучения"
  links={[
    {
      title: "Создание элементов в React",
      url: "https://ru.react.dev/reference/react/createElement",
      description: "Официальная документация по React.createElement API"
    },
    {
      title: "Создание корневого элемента",
      url: "https://ru.react.dev/reference/react-dom/client/createRoot",
      description: "Документация по созданию корневого элемента React приложения"
    },
    {
      title: "Введение в React",
      url: "https://ru.react.dev/learn",
      description: "Официальное введение в React от команды разработчиков"
    }
  ]}
/>

</TaskLayout> 