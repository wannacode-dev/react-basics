<TaskLayout 
  title="React элементы"
  description="Изучаем создание отдельных React элементов и их переиспользование. Это важный шаг к пониманию компонентного подхода."
>

<TaskObjective>

Модифицируйте предыдущий код, создав отдельные React элементы и используя их внутри главного контейнера.

</TaskObjective>

<Info>

**React элемент** — это кусочек интерфейса, который можно сохранить в переменную и переиспользовать.

Основные принципы:
- Элементы можно сохранять в переменные
- Элементы можно переиспользовать многократно
- Для вставки элемента в JSX используются фигурные скобки `{}`
- Это подготовка к изучению React компонентов

Такой подход делает код более модульным и читаемым.

</Info>

<CodeExample title="Создание и использование React элементов">

```javascript
// Переменные для текста
const h1Text = "Привет из React!";
const pText = "Этот текст будет синим";

// Создание отдельных React элементов
const titleElement = <h1 className="title">{h1Text}</h1>;
const textElement = <p className="text">{pText}</p>;

// Использование элементов в контейнере
const container = (
  <div className="container">
    {titleElement}
    {textElement}
  </div>
);

// Пример переиспользования элемента
const button = <button className="btn">Кнопка</button>;

const toolbar = (
  <div className="toolbar">
    {button}
    {button}
    {button}
  </div>
);

// Рендеринг
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(container);
```

</CodeExample>

<TaskLinks 
  title="📚 Материалы для изучения"
  links={[
    {
      title: "Рендеринг элементов",
      url: "https://ru.react.dev/learn/rendering-lists",
      description: "Как создавать и отображать элементы в React"
    },
    {
      title: "Ваш первый компонент",
      url: "https://ru.react.dev/learn/your-first-component", 
      description: "Следующий шаг - создание компонентов"
    }
  ]}
/>

</TaskLayout> 