<TaskLayout 
  title="Вложенные элементы в React"
  description="Изучаем создание иерархии элементов с помощью React.createElement — основу для понимания структуры React приложений."
>

<TaskObjective>

Создайте контейнер с заголовком и параграфом, используя **createElement API**. Научитесь передавать множественные дочерние элементы в React.

</TaskObjective>

<Info>

**Иерархия элементов в React** строится через дочерние элементы:

- Дочерние элементы передаются начиная с 3-го аргумента `createElement`
- Множественные дети можно передать как отдельные аргументы
- Каждый уровень вложенности создается через отдельный `createElement`
- Порядок аргументов: тип элемента, свойства (props), дочерние элементы

React автоматически построит DOM дерево из вложенной структуры элементов.

</Info>

<CodeExample title="Пример создания вложенных элементов">

```javascript
// Создание отдельных элементов
const heading = React.createElement(
  'h1', 
  { className: 'title' }, 
  'Привет из React!'
);

const paragraph = React.createElement(
  'p', 
  { className: 'text' }, 
  'Этот текст будет синим'
);

// Объединение в контейнер
const container = React.createElement(
  'div',
  { className: 'container' },
  heading,     // первый дочерний элемент
  paragraph    // второй дочерний элемент
);

// Альтернативный способ - через массив
const containerWithArray = React.createElement(
  'div',
  { className: 'container' },
  [heading, paragraph]  // массив дочерних элементов
);

// Рендеринг
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(container);
```

</CodeExample>

<TaskLinks 
  title="📚 Материалы для изучения"
  links={[
    {
      title: "Создание элементов с дочерними",
      url: "https://ru.react.dev/reference/react/createElement",
      description: "Полная документация по React.createElement API"
    },
    {
      title: "Понимание React элементов",
      url: "https://ru.react.dev/reference/react/isValidElement",
      description: "Документация о структуре и типах React элементов"
    },
    {
      title: "От JSX к createElement",
      url: "https://ru.react.dev/learn/writing-markup-with-jsx",
      description: "Как JSX превращается в вызовы createElement"
    }
  ]}
/>

</TaskLayout> 