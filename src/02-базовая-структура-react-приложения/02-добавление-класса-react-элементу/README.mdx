<TaskLayout 
  title="Добавление класса React элементу"
  description="Изучаем ключевое отличие React от HTML — использование className вместо class для добавления CSS классов к элементам."
>

<TaskObjective>

Создайте React элемент с CSS классом, используя свойство **className**. Элемент должен иметь заголовок "Привет из React!" с красным цветом текста.

</TaskObjective>

<Info>

**className vs class** — важное отличие React от HTML:

- В HTML используется атрибут `class` для CSS классов
- В React используется `className` по техническим причинам  
- `class` зарезервировано в JavaScript для создания классов объектов
- React автоматически преобразует `className` в атрибут `class` в итоговом HTML

Второй параметр `React.createElement()` — это объект со свойствами элемента (props).

</Info>

<CodeExample title="Пример использования className">

```javascript
// Создание React элемента с CSS классом
const element = React.createElement(
  'h1',                        // тип HTML элемента
  { className: 'title' },      // объект props с CSS классом
  'Привет из React!'           // содержимое элемента
);

// Для множественных классов
const elementWithMultipleClasses = React.createElement(
  'h1',
  { className: 'title large red' },  // несколько классов через пробел
  'Заголовок с несколькими классами'
);

// CSS стили остаются обычными
/*
.title {
  color: red;
  font-size: 24px;
}
*/
```

</CodeExample>

<TaskLinks 
  title="📚 Материалы для изучения"
  links={[
    {
      title: "Создание элементов с атрибутами",
      url: "https://ru.react.dev/reference/react/createElement",
      description: "Полная документация по React.createElement API"
    },
    {
      title: "Применение CSS стилей в React",
      url: "https://ru.react.dev/reference/react-dom/components/common#applying-css-styles",
      description: "Документация по применению CSS стилей в React компонентах"
    },
    {
      title: "Классы в JavaScript",
      url: "https://developer.mozilla.org/ru/docs/Web/JavaScript/Reference/Classes",
      description: "Почему class зарезервировано в JavaScript"
    }
  ]}
/>

</TaskLayout>
