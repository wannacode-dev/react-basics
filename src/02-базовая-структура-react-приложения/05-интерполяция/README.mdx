<TaskLayout 
  title="Интерполяция в React"
  description="Изучаем использование переменных внутри JSX с помощью фигурных скобок. Это делает код более гибким и динамичным."
>

<TaskObjective>

Модифицируйте код из предыдущего задания, используя переменные внутри JSX вместо жестко закодированного текста.

</TaskObjective>

<Info>

**Интерполяция в JSX** позволяет вставлять JavaScript выражения прямо в разметку с помощью фигурных скобок `{}`.

Что можно использовать внутри `{}`:
- Переменные: `{userName}`
- Выражения: `{2 + 2}`
- Функции: `{getName()}`
- Свойства объектов: `{user.name}`
- Условные операторы: `{isOnline ? "В сети" : "Оффлайн"}`

React автоматически преобразует значения в строки для отображения.

</Info>

<CodeExample title="Примеры интерполяции в JSX">

```javascript
// Создание переменных
const title = "Привет из React!";
const description = "Этот текст будет синим";
const user = { name: "Анна", age: 25 };

// Использование в JSX
const element = (
  <div className="container">
    <h1 className="title">{title}</h1>
    <p className="text">{description}</p>
    <p>Привет, {user.name}!</p>
    <p>Возраст: {user.age} лет</p>
    <p>Результат: {2 + 3}</p>
    <p>Статус: {user.age >= 18 ? "Взрослый" : "Ребенок"}</p>
  </div>
);

// Для текущего задания достаточно простых переменных
const simpleElement = (
  <div className="container">
    <h1 className="title">{title}</h1>
    <p className="text">{description}</p>
  </div>
);
```

</CodeExample>

<TaskLinks 
  title="📚 Материалы для изучения"
  links={[
    {
      title: "JavaScript в JSX с фигурными скобками",
      url: "https://ru.react.dev/learn/javascript-in-jsx-with-curly-braces",
      description: "Полное руководство по использованию JavaScript в JSX"
    },
    {
      title: "Условное отображение элементов",
      url: "https://ru.react.dev/learn/conditional-rendering",
      description: "Как отображать элементы по условию в React"
    }
  ]}
/>

</TaskLayout> 