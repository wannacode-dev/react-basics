<TaskLayout 
  title="Добавление CSS классов через JavaScript"
  description="Управление стилями элементов через добавление CSS классов. Понимание classList API поможет понять, как React применяет стили к компонентам."
>

<TaskObjective>

Создайте HTML заголовок с CSS классом "title" и добавьте его на страницу. Элемент должен стать красным благодаря применению стиля.

</TaskObjective>

<TaskConcepts title="Пример кода для изучения">

**Работа с CSS классами через JavaScript:**

```javascript
// Создание элемента
const element = document.createElement('h1')
element.textContent = 'Текст элемента'

// Добавление CSS класса (современный способ)
element.classList.add('my-class')

// Альтернативный способ через className
element.className = 'my-class'

// Множественные классы
element.classList.add('class1', 'class2')
```

**Основные методы classList:**
- `add()` — добавляет класс
- `remove()` — удаляет класс
- `toggle()` — переключает класс
- `contains()` — проверяет наличие класса

**CSS класс "title" уже определен в стилях:**
```css
.title { color: red; }
```

</TaskConcepts>

<TaskLinks 
  title="📚 Материалы для изучения"
  links={[
    {
      title: "Как добавить класс элементу",
      url: "https://developer.mozilla.org/ru/docs/Web/API/Element/classList",
      description: "Современный API для работы с CSS классами"
    },
    {
      title: "Как установить класс через className",
      url: "https://developer.mozilla.org/ru/docs/Web/API/Element/className",
      description: "Альтернативный способ работы с классами"
    },
    {
      title: "Как управлять стилями в JavaScript",
      url: "https://javascript.info/styles-and-classes",
      description: "Полное руководство по работе со стилями"
    }
  ]}
/>

</TaskLayout>  