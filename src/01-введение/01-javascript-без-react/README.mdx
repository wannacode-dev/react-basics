<TaskLayout 
  title="JavaScript без React"
  description="Создание HTML элементов с помощью чистого JavaScript. Понимание DOM API поможет лучше понять, что делает React автоматически."
>

<TaskObjective>

Создайте HTML заголовок с текстом "Привет из JS!" и добавьте его на страницу, используя только встроенные JavaScript методы.

</TaskObjective>

<TaskConcepts title="Пример кода для изучения">

**Создание элементов с помощью DOM API:**

```javascript
// Создание элемента
const element = document.createElement('h1')

// Установка содержимого
element.textContent = 'Текст элемента'

// Поиск контейнера
const container = document.getElementById('root')

// Добавление в DOM
container.appendChild(element)
```

**Основные методы:**
- `createElement()` — создает элемент в памяти
- `textContent` — устанавливает текст безопасно  
- `getElementById()` — ищет элемент по ID
- `appendChild()` — добавляет дочерний элемент

</TaskConcepts>

<TaskLinks 
  title="📚 Материалы для изучения"
  links={[
    {
      title: "Как создать HTML элемент",
      url: "https://developer.mozilla.org/ru/docs/Web/API/Document/createElement",
      description: "Создание элементов с помощью document.createElement"
    },
    {
      title: "Как добавить элемент на страницу",
      url: "https://developer.mozilla.org/ru/docs/Web/API/Node/appendChild",
      description: "Добавление элементов в DOM дерево"
    },
    {
      title: "Как установить текст элемента",
      url: "https://developer.mozilla.org/ru/docs/Web/API/Node/textContent",
      description: "Безопасная установка текстового содержимого"
    }
  ]}
/>

</TaskLayout>
