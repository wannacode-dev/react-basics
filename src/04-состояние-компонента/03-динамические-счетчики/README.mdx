<TaskLayout 
  title="Динамические счетчики"
  description="Работа с массивами в состоянии и динамическое создание компонентов. Изучаем как состояние может хранить массивы данных."
>

<TaskObjective>

Добавьте возможность создавать новые счетчики по нажатию кнопки, используя массив в состоянии.

</TaskObjective>

<Info>

**Массивы в состоянии** — мощный инструмент для динамического управления компонентами:

- Состояние может хранить массивы любых данных
- React требует **иммутабельности** — создания новых массивов вместо изменения старых
- Spread оператор `...` помогает создавать копии массивов
- При рендере списков каждый элемент должен иметь уникальный `key`

Это позволяет создавать интерфейсы с динамическим количеством элементов.

</Info>

<CodeExample title="Динамическое создание компонентов">

```jsx
function Counter() {
  const [count, setCount] = useState(0);
  
  return (
    <div>
      <h3>Счетчик: {count}</h3>
      <button onClick={() => setCount(count + 1)}>Увеличить</button>
    </div>
  );
}

function App() {
  // Состояние как массив (изначально 3 счетчика)
  const [counters, setCounters] = useState([0, 0, 0]);
  
  // Функция для добавления нового счетчика
  const addCounter = () => {
    // ✅ Правильно - создаем новый массив с spread оператором
    setCounters([...counters, 0]);
    
    // ❌ Неправильно - мутация существующего массива
    // counters.push(0);
    // setCounters(counters);
  };
  
  return (
    <div>
      <h1>Динамические счетчики</h1>
      
      <button onClick={addCounter}>
        Добавить счетчик
      </button>
      
      <div>
        {/* Рендерим компонент для каждого элемента массива */}
        {counters.map((_, index) => (
          <Counter key={index} />
        ))}
      </div>
      
      <p>Всего счетчиков: {counters.length}</p>
    </div>
  );
}
```

</CodeExample>

<Warning>

**Важные правила работы с массивами:**

- Не мутируйте массивы напрямую — React не заметит изменения
- Используйте `key` при рендере списков для оптимизации
- Spread оператор `...` создает копию массива
- Каждый динамически созданный компонент получает свое независимое состояние

</Warning>

<TaskLinks 
  title="📚 Материалы для изучения"
  links={[
    {
      title: "Обновление массивов в состоянии",
      url: "https://ru.react.dev/learn/updating-arrays-in-state",
      description: "Как правильно работать с массивами в React состоянии"
    },
    {
      title: "Рендеринг списков",
      url: "https://ru.react.dev/learn/rendering-lists",
      description: "Создание динамических списков в React"
    }
  ]}
/>

</TaskLayout> 