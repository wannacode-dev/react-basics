<TaskLayout 
  title="Множественные счетчики"
  description="Создание переиспользуемых компонентов с независимым состоянием. Каждый компонент живет своей жизнью!"
>

<TaskObjective>

Создайте переиспользуемый компонент счетчика и используйте его несколько раз на странице.

</TaskObjective>

<Info>

**Независимость компонентов** — ключевая особенность React компонентов:

- Каждое использование компонента создает отдельный экземпляр
- Каждый экземпляр имеет собственное изолированное состояние
- React автоматически управляет памятью для каждого экземпляра
- изменения в одном экземпляре не влияют на другие

Это позволяет создавать полностью переиспользуемые компоненты.

</Info>

<CodeExample title="Переиспользуемые компоненты с состоянием">

```jsx
function Counter() {
  // Каждый экземпляр получает собственное состояние
  const [count, setCount] = useState(0);
  
  const handleIncrement = () => {
    setCount(count + 1);
  };
  
  return (
    <div>
      <h3>Счетчик: {count}</h3>
      <button onClick={handleIncrement}>Увеличить</button>
    </div>
  );
}

function App() {
  return (
    <div>
      <h1>Множественные независимые счетчики</h1>
      
      {/* Каждый Counter - отдельный экземпляр с собственным состоянием */}
      <Counter />  {/* Экземпляр #1 */}
      <Counter />  {/* Экземпляр #2 */}
      <Counter />  {/* Экземпляр #3 */ }
      
      <p>Каждый счетчик работает независимо!</p>
    </div>
  );
}
```

</CodeExample>

<TaskLinks 
  title="📚 Материалы для изучения"
  links={[
    {
      title: "Переиспользование компонентов",
      url: "https://ru.react.dev/learn/passing-props-to-a-component",
      description: "Как создавать и использовать React компоненты"
    },
    {
      title: "Независимое состояние компонентов",
      url: "https://ru.react.dev/learn/state-a-components-memory",
      description: "Как компоненты запоминают информацию независимо друг от друга"
    }
  ]}
/>

</TaskLayout> 