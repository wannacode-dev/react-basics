<TaskLayout 
  title="Расширенный счетчик"
  description="Несколько состояний в одном компоненте и инициализация через props. Компонент может помнить много разных вещей одновременно!"
>

<TaskObjective>

Создайте счетчик с множественными состояниями: значение счетчика, увелечение на 1, на 2, умножение на 2, блокировка кнопок, сброс счетчика.

</TaskObjective>

<Info>

**Множественные состояния** — компонент может иметь несколько независимых состояний:

- Каждый вызов `useState` создает отдельное состояние
- Состояния не связаны друг с другом и обновляются независимо
- Props можно использовать для задания начальных значений состояний

Это позволяет создавать сложные интерактивные компоненты.

</Info>

<CodeExample title="Компонент с множественными состояниями">

```jsx
function AdvancedCounter({ initialCount = 0 }) {
  // Состояние #1: значение счетчика
  const [count, setCount] = useState(initialCount);
  
  // Состояние #2: заблокированы ли кнопки
  const [isDisabled, setIsDisabled] = useState(false);
  
  // Различные операции со счетчиком
  const increment = () => setCount(count + 1);
  const incrementBy2 = () => setCount(count + 2);
  const multiplyBy2 = () => setCount(count * 2);
  const reset = () => setCount(0);
  
  // Переключение блокировки
  const toggleDisabled = () => setIsDisabled(!isDisabled);
  
  return (
    <div>
      <h2>Счетчик: {count}</h2>
      
      <div>
        {/* Кнопки, которые блокируются */}
        <button onClick={increment} disabled={isDisabled}>
          +1
        </button>
        
        <button onClick={incrementBy2} disabled={isDisabled}>
          +2
        </button>
        
        <button onClick={multiplyBy2} disabled={isDisabled}>
          ×2
        </button>
        
        {/* Кнопки, которые всегда активны */}
        <button onClick={toggleDisabled}>
          {isDisabled ? 'Разблокировать' : 'Блокировать'}
        </button>
        
        <button onClick={reset}>
          Сброс
        </button>
      </div>
      
      <p>Статус: {isDisabled ? 'Заблокировано' : 'Активно'}</p>
    </div>
  );
}

function App() {
  return (
    <div>
      <h1>Расширенные счетчики</h1>
      
      {/* Разные счетчики с разными начальными значениями */}
      <AdvancedCounter initialCount={0} />
      <AdvancedCounter initialCount={5} />
      <AdvancedCounter initialCount={10} />
    </div>
  );
}
```

</CodeExample>

<Warning>

**Важные принципы множественных состояний:**

- Используйте отдельное состояние для каждого параметра: загрузка, блокировка и т.п.
- Props для инициализации состояния используйте только при создании компонента

</Warning>

<TaskLinks 
  title="📚 Материалы для изучения"
  links={[
    {
      title: "Состояние как память компонента",
      url: "https://ru.react.dev/learn/state-a-components-memory",
      description: "Как компоненты помнят информацию с помощью состояния"
    },
    {
      title: "Передача props в компоненты",
      url: "https://ru.react.dev/learn/passing-props-to-a-component",
      description: "Передача данных в компоненты через props"
    },
    {
      title: "Справочник по useState",
      url: "https://ru.react.dev/reference/react/useState",
      description: "Полная документация по хуку useState"
    }
  ]}
/>

</TaskLayout> 