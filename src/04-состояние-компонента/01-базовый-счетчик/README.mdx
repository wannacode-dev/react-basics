<TaskLayout 
  title="Базовый счетчик с useState"
  description="Знакомство с состоянием в React! Изучаем useState — хук, который позволяет компонентам «помнить» информацию между перерисовками."
>

<TaskObjective>

Создайте интерактивный счетчик, который увеличивается при нажатии на кнопку, используя React хук **useState**.

</TaskObjective>

<Info>

**useState** — это хук для добавления состояния в функциональные компоненты:

- **Состояние** — это «память» компонента между перерисовками
- **useState(0)** создает состояние с начальным значением 0
- **Возвращает массив** из двух элементов: [значение, функция_обновления]
- **Деструктуризация** позволяет удобно получить оба элемента

Состояние позволяет компонентам реагировать на действия пользователя и обновлять интерфейс.

</Info>

<CodeExample title="Пример использования useState">

```javascript
function Counter() {
  // Получаем useState из React
  const { useState } = React;
  
  // Создаем состояние с начальным значением
  const [count, setCount] = useState(0);
  //      ↑        ↑              ↑
  //  значение  функция    начальное значение

  // Функция для обработки клика
  const handleIncrement = () => {
    setCount(count + 1); // Увеличиваем счетчик на 1
  };

  // Рендерим интерфейс
  return (
    <div>
      <p>Счетчик: {count}</p>
      <button onClick={handleIncrement}>Увеличить</button>
    </div>
  );
}

// Пример других операций со счетчиком
function ExtendedCounter() {
  const { useState } = React;
  const [value, setValue] = useState(0);

  const increment = () => setValue(value + 1);
  const decrement = () => setValue(value - 1);
  const reset = () => setValue(0);

  return (
    <div>
      <h2>Значение: {value}</h2>
      <button onClick={increment}>+1</button>
      <button onClick={decrement}>-1</button>
      <button onClick={reset}>Сброс</button>
    </div>
  );
}
```

</CodeExample>

<Warning>

**Важные правила useState:**

- Никогда не изменяйте состояние напрямую — используйте функцию-сеттер
- Состояние асинхронно — изменения видны только после перерисовки
- Хуки можно вызывать только на верхнем уровне функции компонента

</Warning>

<TaskLinks 
  title="📚 Материалы для изучения"
  links={[
    {
      title: "Хук useState",
      url: "https://ru.react.dev/reference/react/useState",
      description: "Официальная документация по хуку useState"
    },
    {
      title: "Состояние как память компонента",
      url: "https://ru.react.dev/learn/state-a-components-memory", 
      description: "Понимание состояния как памяти компонента"
    },
    {
      title: "Реакция на события",
      url: "https://ru.react.dev/learn/responding-to-events",
      description: "Как обрабатывать события в React компонентах"
    }
  ]}
/>

</TaskLayout> 