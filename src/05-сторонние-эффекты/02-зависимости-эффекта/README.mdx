<TaskLayout 
  title="Зависимости эффекта"
  description="Управление перезапуском useEffect с помощью массива зависимостей. Изучаем, когда и как эффекты должны обновляться."
>

<TaskObjective>

Создайте два компонента: список книг и детальная информация, которая автоматически обновляется при выборе книги.

</TaskObjective>

<Info>

**Массив зависимостей** контролирует, когда useEffect должен перезапускаться:

- React сравнивает каждое значение в массиве с предыдущим рендером
- Если любая зависимость изменилась, эффект перезапускается
- Пустой массив `[]` = эффект выполнится только один раз
- Без массива = эффект выполняется после каждого рендера

Правильные зависимости гарантируют актуальность данных и предотвращают ненужные запросы.

</Info>

<CodeExample title="Использование зависимостей в useEffect">

```jsx
function Example({ name }) {
  const { useState, useEffect } = React;
  const [message, setMessage] = useState('');
  
  // Эффект с зависимостью от name
  useEffect(() => {
    setMessage(`Привет, ${name}!`);
  }, [name]); // Перезапуск при изменении name
  
  return <div>{message}</div>;
}

function App() {
  const { useState } = React;
  const [name, setName] = useState('Анна');
  
  return (
    <div>
      <input 
        value={name} 
        onChange={(e) => setName(e.target.value)}
        placeholder="Введите имя"
      />
      <Example name={name} />
    </div>
  );
}
```

</CodeExample>

<Warning>

**Частые ошибки с зависимостями:**

- **Пропущенная зависимость** — эффект использует устаревшие данные
- **Лишняя зависимость** — ненужные перезапуски эффекта
- **Объекты и массивы как зависимости** — создают новые ссылки каждый рендер
- **Забыли cleanup** — утечки памяти при использовании таймеров или подписок

</Warning>

<TaskLinks 
  title="📚 Материалы для изучения"
  links={[
    {
      title: "Синхронизация с эффектами",
      url: "https://ru.react.dev/learn/synchronizing-with-effects",
      description: "Полное руководство по синхронизации с эффектами"
    },
    {
      title: "Зависимости useEffect",
      url: "https://ru.react.dev/reference/react/useEffect#parameters",
      description: "Документация по зависимостям useEffect"
    },
    {
      title: "Опциональная цепочка в JavaScript",
      url: "https://developer.mozilla.org/ru/docs/Web/JavaScript/Reference/Operators/Optional_chaining",
      description: "Как использовать опциональную цепочку в JavaScript"
    }
  ]}
/>

</TaskLayout> 