<TaskLayout 
  title="Переиспользуемые компоненты"
  description="Изучаем многократное использование одного компонента с разными данными. Демонстрируем мощь переиспользуемости React компонентов."
>

<TaskObjective>

Продемонстрируйте мощь переиспользуемости, используя компоненты `Title` и `Text` многократно с разными текстами.

</TaskObjective>

<Info>

**Переиспользуемость** — одно из главных преимуществ React компонентов:

- Один компонент можно использовать многократно с разными данными
- Принцип DRY (Don't Repeat Yourself) — не повторяйте код
- Компоненты как шаблоны — один шаблон, множество экземпляров
- Централизованные изменения — обновили компонент, обновилось везде

Это делает код более чистым, надежным и простым в поддержке.

</Info>

<CodeExample title="Переиспользование компонентов">

```javascript
// Создаем компоненты один раз
function Title({ text }) {
  return <h1 className="title">{text}</h1>;
}

function Text({ text }) {
  return <p className="text">{text}</p>;
}

function Container({ children }) {
  return <div className="container">{children}</div>;
}

// Подготавливаем разные данные
const firstTitleText = "Это мой первый компонент!";
const firstDescriptionText = "Я создал его сам!";

const secondTitleText = "Это мой второй компонент!";
const secondDescriptionText = "Тоже я создал его сам!";

const thirdTitleText = "Это мой третий компонент!";
const thirdDescriptionText = "И этот тоже сам!";

// Используем компоненты многократно
function App() {
  return (
    <Container>
      {/* Первая пара компонентов */}
      <Title text={firstTitleText} />
      <Text text={firstDescriptionText} />
      
      {/* Вторая пара компонентов */}
      <Title text={secondTitleText} />
      <Text text={secondDescriptionText} />
      
      {/* Третья пара компонентов */}
      <Title text={thirdTitleText} />
      <Text text={thirdDescriptionText} />
    </Container>
  );
}

// Рендеринг
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);
```

</CodeExample>

<TaskLinks 
  title="📚 Материалы для изучения"
  links={[
    {
      title: "Ваш первый компонент",
      url: "https://ru.react.dev/learn/your-first-component",
      description: "Основы создания React компонентов"
    },
    {
      title: "Передача props в компонент",
      url: "https://ru.react.dev/learn/passing-props-to-a-component",
      description: "Как передавать данные в компоненты для переиспользования"
    }
  ]}
/>

</TaskLayout> 