<TaskLayout 
  title="React элементы в виде функций"
  description="Первый шаг к созданию React компонентов! Изучаем создание функций, которые возвращают React элементы."
>

<TaskObjective>

Преобразуйте код из предыдущего задания в переиспользуемые функции, каждая из которых возвращает React элемент.

</TaskObjective>

<Info>

**Функции как фабрики элементов** — основа компонентной архитектуры React:

- Функция принимает параметры и использует их в JSX
- Возвращает React элемент с помощью `return`
- Можно вызывать многократно с разными параметрами
- Обеспечивает переиспользуемость кода

Это промежуточный шаг перед изучением полноценных React компонентов.

</Info>

<CodeExample title="Создание функций для элементов">

```javascript
// Функция для создания заголовка
function Title(text) {
  return <h1 className="title">{text}</h1>;
}

// Функция для создания параграфа  
function Text(text) {
  return <p className="text">{text}</p>;
}

// Функция для создания контейнера
function Container(children) {
  return <div className="container">{children}</div>;
}

// Использование функций
const h1Text = "Привет из React!";
const pText = "Этот текст синий";

// Создаем элементы через функции
const title = Title(h1Text);
const text = Text(pText);
const container = Container([title, text]);

// Рендеринг
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(container);
```

</CodeExample>

<TaskLinks 
  title="📚 Материалы для изучения"
  links={[
    {
      title: "Ваш первый компонент",
      url: "https://ru.react.dev/learn/your-first-component",
      description: "Официальная документация React о создании первого компонента"
    },
    {
      title: "Основы функций в JavaScript",
      url: "https://developer.mozilla.org/ru/docs/Web/JavaScript/Guide/Functions",
      description: "Основы работы с функциями в JavaScript"
    }
  ]}
/>

</TaskLayout> 