<TaskLayout 
  title="Props компонента"
  description="Изучаем передачу данных в React компоненты через props. Props делают компоненты гибкими и переиспользуемыми."
>

<TaskObjective>

Создайте компоненты, которые могут отображать разный контент в зависимости от переданных данных через props.

</TaskObjective>

<Info>

**Props** (properties) — это способ передачи данных от родительского компонента к дочернему:

- Props передаются как атрибуты компонента: `<Title text="Привет!" />`
- Внутри компонента доступны через параметр: `function Title(props)`
- Удобно использовать деструктуризацию: `function Title({ text })`
- Props только для чтения — их нельзя изменять внутри компонента

Это делает компоненты настраиваемыми и переиспользуемыми.

</Info>

<CodeExample title="Компоненты с props">

```javascript
// Компонент с props (классический способ)
function Title(props) {
  return <h1 className="title">{props.text}</h1>;
}

// Компонент с деструктуризацией props (современный способ)
function Text({ text }) {
  return <p className="text">{text}</p>;
}

// Компонент контейнера
function Container() {
  return (
    <div className="container">
      <Title text="Привет из React компонента!" />
      <Text text="Это текст из компонента P" />
    </div>
  );
}

// Демонстрация переиспользуемости
function App() {
  return (
    <div>
      <Container />
      
      {/* Те же компоненты с другими данными */}
      <Title text="Другой заголовок!" />
      <Text text="И другой текст." />
      
      <Title text="Еще один заголовок" />
      <Text text="Один компонент — множество применений!" />
    </div>
  );
}

// Рендеринг
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);
```

</CodeExample>

<TaskLinks 
  title="📚 Материалы для изучения"
  links={[
    {
      title: "Передача props в компонент",
      url: "https://ru.react.dev/learn/passing-props-to-a-component",
      description: "Подробное руководство по работе с props в React"
    },
    {
      title: "Компоненты и props",
      url: "https://ru.react.dev/learn/your-first-component#props",
      description: "Основы компонентов и работы с props"
    }
  ]}
/>

</TaskLayout> 