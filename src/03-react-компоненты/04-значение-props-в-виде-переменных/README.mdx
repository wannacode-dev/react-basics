<TaskLayout 
  title="Значения props в виде переменных"
  description="Изучаем передачу динамических данных в компоненты через переменные вместо статичных строк."
>

<TaskObjective>

Модифицируйте код из предыдущего задания, чтобы использовать переменные для значений props вместо жестко заданных строк.

</TaskObjective>

<Info>

**Динамические данные в props** позволяют делать компоненты более гибкими:

- Переменные объявляются до использования в JSX
- Строки передаются в кавычках: `text="Привет"`
- Переменные передаются в фигурных скобках: `text={переменная}`
- В скобках можно использовать любые JavaScript выражения

Это позволяет централизованно управлять данными и легко их изменять.

</Info>

<CodeExample title="Использование переменных в props">

```javascript
// Создание переменных для данных
const titleText = "Привет из React компонента!";
const descriptionText = "Это текст из компонента P";

// Компоненты остаются без изменений
function Title({ text }) {
  return <h1 className="title">{text}</h1>;
}

function Text({ text }) {
  return <p className="text">{text}</p>;
}

function Container() {
  return (
    <div className="container">
      <Title text={titleText} />
      <Text text={descriptionText} />
    </div>
  );
}

// Примеры других способов передачи данных
const user = { name: "Анна", age: 25 };

function Examples() {
  return (
    <div>
      {/* Статичная строка */}
      <Title text="Статичный заголовок" />
      
      {/* Переменная */}
      <Title text={titleText} />
      
      {/* Свойство объекта */}
      <Title text={user.name} />
      
      {/* JavaScript выражение */}
      <Title text={user.name.toUpperCase()} />
    </div>
  );
}

// Рендеринг
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<Container />);
```

</CodeExample>

<TaskLinks 
  title="📚 Материалы для изучения"
  links={[
    {
      title: "JavaScript в JSX с фигурными скобками",
      url: "https://ru.react.dev/learn/javascript-in-jsx-with-curly-braces",
      description: "Использование JavaScript выражений в JSX"
    },
    {
      title: "Передача props в компонент",
      url: "https://ru.react.dev/learn/passing-props-to-a-component",
      description: "Как передавать данные в компоненты"
    }
  ]}
/>

</TaskLayout> 