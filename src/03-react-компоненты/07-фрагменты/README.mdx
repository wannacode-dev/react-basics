<TaskLayout 
  title="Фрагменты в React"
  description="Изучаем использование React.Fragment для группировки элементов без создания лишних DOM-узлов."
>

<TaskObjective>

Замените обертку `div` на React Fragment для создания более чистого HTML-кода без лишних оберток.

</TaskObjective>

<Info>

**React Fragment** — специальный компонент для группировки элементов без создания DOM-узла:

- Группирует элементы как div, но не создает дополнительный элемент в HTML
- Два способа записи: краткая `<> </>` и полная `<React.Fragment>`
- Полезен для создания чистого HTML без лишней вложенности
- Особенно важен при работе со списками и таблицами

Это помогает избежать "div-супа" в итоговом HTML.

</Info>

<CodeExample title="Использование React Fragment">

```javascript
// Обычный способ с div (создает лишний элемент)
function WithDiv() {
  return (
    <div>
      <h1 className="title">Привет из React компонента!</h1>
      <p className="text">Это текст из компонента P</p>
    </div>
  );
}

// С React Fragment (краткая запись - рекомендуется)
function WithFragment() {
  return (
    <>
      <h1 className="title">Привет из React компонента!</h1>
      <p className="text">Это текст из компонента P</p>
    </>
  );
}

// С React Fragment (полная запись)
function WithFragmentFull() {
  return (
    <React.Fragment>
      <h1 className="title">Привет из React компонента!</h1>
      <p className="text">Это текст из компонента P</p>
    </React.Fragment>
  );
}

// Результат в HTML:
// С div: <div><h1>...</h1><p>...</p></div>
// С Fragment: <h1>...</h1><p>...</p>

// Рендеринг
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<WithFragment />);
```

</CodeExample>

<TaskLinks 
  title="📚 Материалы для изучения"
  links={[
    {
      title: "React Fragment",
      url: "https://ru.react.dev/reference/react/Fragment",
      description: "Полное руководство по использованию React Fragment"
    },
    {
      title: "Группировка компонентов",
      url: "https://ru.react.dev/learn/rendering-lists#keeping-list-items-in-order-with-key",
      description: "Как группировать компоненты без лишних элементов"
    }
  ]}
/>

</TaskLayout> 