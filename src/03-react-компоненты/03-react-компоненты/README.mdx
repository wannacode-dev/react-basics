<TaskLayout 
  title="React компоненты"
  description="Превращение функций в настоящие React компоненты. Изучаем создание переиспользуемых компонентов с заглавной буквы."
>

<TaskObjective>

Преобразуйте обычные функции в полноценные React компоненты, которые можно использовать как HTML теги.

</TaskObjective>

<Info>

**React компонент** — это функция, которая:
- Начинается с заглавной буквы (обязательно!)
- Возвращает JSX элемент
- Используется как HTML тег `<Component />`

Основные отличия от обычных функций:
- Именование с большой буквы помогает React отличать компоненты от HTML тегов
- Вместо вызова как функция `title()` используем как тег `<Title />`
- React автоматически вызывает функцию компонента при рендеринге

</Info>

<CodeExample title="Превращение функций в компоненты">

```javascript
// Обычные функции (предыдущий урок)
function title() {
  return <h1 className="title">Привет из React!</h1>;
}

function text() {
  return <p className="text">Этот текст будет синим</p>;
}

// React компоненты (текущий урок)
function Title() {
  return <h1 className="title">Привет из React!</h1>;
}

function Text() {
  return <p className="text">Этот текст будет синим</p>;
}

function Container() {
  return (
    <div className="container">
      <Title />
      <Text />
    </div>
  );
}

// Использование
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<Container />);
```

</CodeExample>

<Warning>

**Важно!** Имя компонента ВСЕГДА должно начинаться с заглавной буквы:

- ✅ `function Title()` — правильно
- ❌ `function title()` — неправильно, React будет искать HTML тег `<title>`

</Warning>

<TaskLinks 
  title="📚 Материалы для изучения"
  links={[
    {
      title: "Ваш первый компонент",
      url: "https://ru.react.dev/learn/your-first-component",
      description: "Создание вашего первого React компонента"
    },
    {
      title: "Импорт и экспорт компонентов",
      url: "https://ru.react.dev/learn/importing-and-exporting-components",
      description: "Как организовать компоненты в файлах"
    },
    {
      title: "Написание разметки с JSX",
      url: "https://ru.react.dev/learn/writing-markup-with-jsx",
      description: "Написание разметки с использованием JSX"
    }
  ]}
/>

</TaskLayout> 